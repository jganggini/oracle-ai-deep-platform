# Puertos estándar del stack de monitoreo
PROMETHEUS_PORT=8002
GRAFANA_PORT=8003

# Credenciales de Grafana (solo para desarrollo)
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin

# Red externa a la que se conectará el stack (por ejemplo, la del OCR)
# Si el stack OCR está arriba, usa su red (p.ej., docker-ocr_default)
# Si no existe, créala manualmente: docker network create docker-ocr_default
EXTERNAL_NETWORK=docker-mineru_default

# Agrega endpoints en archivos targets/*.yml (ver README). No se requiere variable aquí.

